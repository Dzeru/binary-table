<!DOCTYPE html>
<html xmlns:=http://www.w3.org/1999/xhtml">
<head>
    <title>BiT | Goals</title>
</head>
<body>
    <header>
        <div> <strong><a href="/">BiT</a></strong></div>

        <div>{{#user}}
            {{id}}&nbsp;{{name}}&nbsp;{{username}}&nbsp;{{password}}
            {{/user}}
        </div>

        <div>
            {{countOfGoals}}
        </div>

        <div>
            <form action="/newgoal" method="get">
                <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
                <div><input type="submit" value="Add goal"/></div>
            </form>
        </div>

        <div>
            <form action="/logout" method="post">
                <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
                <input type="submit" value="Log Out"/>
            </form>
        </div>
    </header>

    <br>
    <div>
        {{#goals}}
            <div>
                <a href="goal?id={{id}}">{{goalName}}&nbsp;{{doneSteps}}/{{allSteps}}</a>
            </div>
        {{/goals}}

        {{^goals}}
            There are no goals yet!
        {{/goals}}
    </div>

</body>
</html>